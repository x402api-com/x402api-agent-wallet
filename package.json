{
  "name": "x402api-agent-wallet-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "Persistent local agent wallet CLI and skill for x402 payments.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @x402api/agent-wallet-core && npm run build --workspace @x402api/agent-wallet-cli",
    "typecheck": "npm run build --workspace @x402api/agent-wallet-core && npm run typecheck --workspace @x402api/agent-wallet-core && npm run typecheck --workspace @x402api/agent-wallet-cli",
    "test": "npm run build --workspace @x402api/agent-wallet-core && npm run test --workspaces --if-present",
    "test:coverage": "npm run build --workspace @x402api/agent-wallet-core && npm run test:coverage --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present && node tools/validate-repository.mjs",
    "pack:smoke": "node tools/pack-smoke.mjs",
    "validate:skill": "node tools/validate-skill.mjs skills/x402api-pay",
    "check": "npm run lint && npm run typecheck && npm test && npm run build && npm run validate:skill && npm run pack:smoke"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
